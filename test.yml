---
  - hosts: all
    gather_facts: False
    become: True

    tasks:
      - name: test
        debug:
          msg: "{{ item.key }} mount to  {{ item.value.directory.split('/')[-1] }}"
        with_dict:
          - "{{ mountmap }}"

    #  - name: test2
    #    debug:
    #      msg: "{{ devices.index(item) }}"
    #    with_items:
    #      - "{{ devices }}"
